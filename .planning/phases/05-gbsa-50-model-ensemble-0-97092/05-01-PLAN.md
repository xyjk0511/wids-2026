# Plan 05-01: 精确复现 0.97092 特征工程

**Phase:** 5 (GBSA 50-Model Ensemble)
**Created:** 2026-02-28
**Status:** Ready for execution

---

## Objective

精确复现 0.97092 notebook 的 `create_features()` 函数，实现 30+ 距离导向特征工程，替换当前 exp34 使用的 medium 特征集（36+6 augmented），消除特征集差异导致的性能 gap。

---

## Context

**Why this plan is needed:**

Exp34 的 LB 0.96600 低于 0.97092 的根本原因之一是特征集完全不匹配：
- **0.97092**: 30+ 距离导向特征（log_dist, dist_area_ratio, eta_hours, inv_dist, dist_alignment 等）
- **Exp34**: medium 特征集（36 base + 6 engineered），包含大量冗余特征

从 05-CONTEXT.md 确认：特征工程是最大差异源，必须对齐才能复现 0.97092。

**Current state:**
- `src/features.py` 的 `add_engineered()` 仅有 6 个简单特征
- `exp34_gbsa_ensemble.py` 使用 `remove_redundant()` + `add_engineered()` 生成 medium 特征集
- 缺少 0.97092 的核心距离导向特征

---

## Requirements

- **GBSA-01** (partial): 实现 0.97092 特征工程作为 GBSA ensemble 的基础

---

## Implementation Steps

### Step 1: 提取 0.97092 特征工程代码

**File:** `src/features.py`

**Action:** 在 `add_engineered()` 函数中添加 0.97092 的完整特征集

**Code to add:**

```python
def add_engineered_0_97092(df: pd.DataFrame) -> pd.DataFrame:
    """Add 0.97092 distance-oriented features (30+ features)."""
    df = df.copy()

    # === Core distance features ===
    # log-distance: compress long-tail distribution
    df["log_dist"] = np.log1p(df["dist_min_ci_0_5h"])

    # inverse distance: amplifies near-range discrimination
    dist_safe = df["dist_min_ci_0_5h"].clip(lower=1.0)
    df["inv_dist"] = 1.0 / dist_safe

    # binary: fire already within 5km threshold
    df["dist_close"] = (df["dist_min_ci_0_5h"] < 5000).astype(int)

    # === Distance-area interactions ===
    # distance-area ratio: closer + larger fire = higher threat
    area_sqrt = np.sqrt(df["area_first_ha"].clip(lower=1e-6))
    df["dist_area_ratio"] = dist_safe / area_sqrt

    # area-weighted inverse distance
    df["inv_dist_area"] = df["inv_dist"] * area_sqrt

    # === Distance-speed interactions ===
    # closing speed relative to distance
    speed_safe = df["closing_speed_m_per_h"].clip(lower=0.1)
    df["speed_dist_ratio"] = speed_safe / dist_safe

    # ETA: estimated time of arrival (distance / closing speed)
    df["eta_hours"] = dist_safe / speed_safe

    # log ETA for long-tail compression
    df["log_eta"] = np.log1p(df["eta_hours"])

    # === Distance-alignment interactions ===
    # distance x alignment interaction
    df["dist_alignment"] = df["log_dist"] * df["alignment_abs"]

    # inverse distance x alignment
    df["inv_dist_alignment"] = df["inv_dist"] * df["alignment_abs"]

    # === Distance-growth interactions ===
    # area growth rate relative to distance
    df["growth_dist_ratio"] = df["area_growth_rate_ha_per_h"] / dist_safe

    # binary growth flag
    df["has_growth"] = (df["area_growth_abs_0_5h"] > 0).astype(int)

    # growth-weighted inverse distance
    df["inv_dist_growth"] = df["inv_dist"] * (1 + df["area_growth_rate_ha_per_h"])

    # === Distance-wind interactions ===
    # wind speed relative to distance
    wind_safe = df["wind_speed_m_per_s"].clip(lower=0.1)
    df["wind_dist_ratio"] = wind_safe / dist_safe

    # wind-weighted inverse distance
    df["inv_dist_wind"] = df["inv_dist"] * wind_safe

    # === Distance-temperature interactions ===
    # temperature relative to distance (hotter = faster spread)
    temp_safe = df["temp_c"].clip(lower=0.1)
    df["temp_dist_ratio"] = temp_safe / dist_safe

    # === Distance-humidity interactions ===
    # humidity relative to distance (drier = faster spread)
    humidity_safe = df["humidity_percent"].clip(lower=1.0)
    df["humidity_dist_ratio"] = humidity_safe / dist_safe

    # inverse humidity (dryness) weighted by inverse distance
    df["inv_dist_dryness"] = df["inv_dist"] * (100.0 / humidity_safe)

    # === Composite threat scores ===
    # threat score 1: distance + speed + area
    df["threat_score_1"] = df["inv_dist"] * df["speed_dist_ratio"] * area_sqrt

    # threat score 2: distance + growth + alignment
    df["threat_score_2"] = df["inv_dist"] * df["growth_dist_ratio"] * df["alignment_abs"]

    # threat score 3: distance + wind + dryness
    df["threat_score_3"] = df["inv_dist"] * df["wind_dist_ratio"] * (100.0 / humidity_safe)

    # === Distance bins (categorical encoding) ===
    # distance bins: 0-1km, 1-2km, 2-5km, 5-10km, 10km+
    df["dist_bin"] = pd.cut(
        df["dist_min_ci_0_5h"],
        bins=[0, 1000, 2000, 5000, 10000, np.inf],
        labels=[0, 1, 2, 3, 4],
    ).astype(int)

    # === Log-scale features for skewed distributions ===
    # log area
    df["log_area"] = np.log1p(df["area_first_ha"])

    # log closing speed
    df["log_speed"] = np.log1p(df["closing_speed_m_per_h"])

    # log wind speed
    df["log_wind"] = np.log1p(df["wind_speed_m_per_s"])

    return df
```

**Verification:**
```python
# Test feature count
train = pd.read_csv(TRAIN_PATH)
train_eng = add_engineered_0_97092(train)
print(f"Original features: {len(train.columns)}")
print(f"Engineered features: {len(train_eng.columns)}")
print(f"New features: {len(train_eng.columns) - len(train.columns)}")
# Expected: ~30 new features
```

---

### Step 2: 更新 exp34 使用新特征集

**File:** `scripts/exp34_gbsa_ensemble.py`

**Action:** 修改 `load_data()` 函数使用 0.97092 特征集

**Changes:**

```python
# OLD (line 64-76):
def load_data(feature_level=None):
    """Load and prepare train/test data."""
    feature_level = feature_level or FEATURE_LEVEL
    train = pd.read_csv(TRAIN_PATH)
    test = pd.read_csv(TEST_PATH)

    if feature_level == "v96624":
        train = add_engineered(train)
        test = add_engineered(test)
    else:  # medium
        train = add_engineered(remove_redundant(train))
        test = add_engineered(remove_redundant(test))

    return train, test

# NEW:
def load_data(feature_level=None):
    """Load and prepare train/test data."""
    feature_level = feature_level or FEATURE_LEVEL
    train = pd.read_csv(TRAIN_PATH)
    test = pd.read_csv(TEST_PATH)

    if feature_level == "0.97092":
        # 0.97092 feature set: no redundant removal, use distance-oriented features
        from src.features import add_engineered_0_97092
        train = add_engineered_0_97092(train)
        test = add_engineered_0_97092(test)
    elif feature_level == "v96624":
        train = add_engineered(train)
        test = add_engineered(test)
    else:  # medium
        train = add_engineered(remove_redundant(train))
        test = add_engineered(remove_redundant(test))

    return train, test
```

**Update global config (line 61):**

```python
# OLD:
FEATURE_LEVEL = "medium"

# NEW:
FEATURE_LEVEL = "0.97092"
```

---

### Step 3: 提取特征列表

**File:** `scripts/exp34_gbsa_ensemble.py`

**Action:** 在 `load_data()` 后自动提取特征列

**Add after load_data():**

```python
def get_feature_cols(train):
    """Extract feature columns (exclude ID, time, event)."""
    exclude = [ID_COL, TIME_COL, EVENT_COL]
    return [c for c in train.columns if c not in exclude]
```

**Update main() to use auto-extracted features:**

```python
# In main():
train, test = load_data(feature_level="0.97092")
feature_cols = get_feature_cols(train)
print(f"Feature count: {len(feature_cols)}")
print(f"Features: {feature_cols[:10]}...")  # Show first 10
```

---

### Step 4: 对比验证特征集差异

**File:** `scripts/compare_features.py` (new)

**Purpose:** 验证 0.97092 特征集与 medium 特征集的差异

**Code:**

```python
"""Compare feature sets: 0.97092 vs medium."""
import pandas as pd
from src.config import TRAIN_PATH
from src.features import remove_redundant, add_engineered, add_engineered_0_97092

# Load data
train = pd.read_csv(TRAIN_PATH)

# Medium feature set
train_medium = add_engineered(remove_redundant(train))
medium_features = [c for c in train_medium.columns if c not in ["id", "time_to_event_hours", "event"]]

# 0.97092 feature set
train_97092 = add_engineered_0_97092(train)
features_97092 = [c for c in train_97092.columns if c not in ["id", "time_to_event_hours", "event"]]

# Compare
print(f"Medium features: {len(medium_features)}")
print(f"0.97092 features: {len(features_97092)}")
print(f"\nOnly in medium: {set(medium_features) - set(features_97092)}")
print(f"\nOnly in 0.97092: {set(features_97092) - set(medium_features)}")
print(f"\nCommon features: {len(set(medium_features) & set(features_97092))}")
```

**Run:**
```bash
python scripts/compare_features.py
```

**Expected output:**
- Medium: ~42 features (36 base + 6 engineered)
- 0.97092: ~46 features (16 base + 30 engineered)
- Only in 0.97092: ~30 distance-oriented features

---

## Verification

### Automated Checks

1. **Feature count validation:**
   ```bash
   python -c "
   import pandas as pd
   from src.config import TRAIN_PATH
   from src.features import add_engineered_0_97092
   train = pd.read_csv(TRAIN_PATH)
   train_eng = add_engineered_0_97092(train)
   new_features = len(train_eng.columns) - len(train.columns)
   assert new_features >= 30, f'Expected >=30 new features, got {new_features}'
   print(f'✓ Feature count: {new_features} new features')
   "
   ```

2. **No NaN/Inf validation:**
   ```bash
   python -c "
   import pandas as pd
   from src.config import TRAIN_PATH
   from src.features import add_engineered_0_97092
   train = pd.read_csv(TRAIN_PATH)
   train_eng = add_engineered_0_97092(train)
   assert not train_eng.isnull().any().any(), 'NaN values found'
   assert not (train_eng == float('inf')).any().any(), 'Inf values found'
   print('✓ No NaN/Inf values')
   "
   ```

3. **Feature comparison:**
   ```bash
   python scripts/compare_features.py
   ```

### Manual Checks

1. **Inspect feature distributions:**
   - `log_dist`: should be log-normal
   - `inv_dist`: should be right-skewed
   - `eta_hours`: should have long tail
   - `threat_score_*`: should be composite scores

2. **Verify feature engineering logic:**
   - `dist_area_ratio = dist / sqrt(area)` (larger area → smaller ratio → higher threat)
   - `speed_dist_ratio = speed / dist` (faster closing → higher ratio → higher threat)
   - `eta_hours = dist / speed` (shorter ETA → higher threat)

---

## Success Criteria

- [ ] `add_engineered_0_97092()` implemented with 30+ features
- [ ] No NaN/Inf values in engineered features
- [ ] `exp34_gbsa_ensemble.py` updated to use 0.97092 feature set
- [ ] Feature comparison script confirms ~30 new distance-oriented features
- [ ] All automated checks pass

**Quantitative gate:**
- Feature count: 45-50 total features (16 base + 30 engineered)
- New features: ≥30 distance-oriented features

---

## Rollback Plan

If feature engineering causes errors or degrades performance:

1. **Immediate rollback:**
   ```python
   # In exp34_gbsa_ensemble.py, line 61:
   FEATURE_LEVEL = "medium"  # Revert to medium
   ```

2. **Preserve 0.97092 features:**
   - Keep `add_engineered_0_97092()` in `src/features.py` for future use
   - Document any issues in `experiments.md`

3. **Fallback strategy:**
   - If 0.97092 features fail, try hybrid: medium base + 0.97092 distance features
   - Incremental feature addition to isolate problematic features

---

## Dependencies

- **Upstream:** None (first plan in Phase 5)
- **Downstream:** 05-02-PLAN.md (GBSA CV-bag needs features), 05-03-PLAN.md (postprocessing needs features)

---

## Estimated Time

- Implementation: 30 minutes
- Verification: 15 minutes
- **Total: 45 minutes**

---

## Notes

- 0.97092 特征工程的核心思想：距离是最强信号，所有特征都围绕距离构建交互项
- 不使用 `remove_redundant()`，保留所有原始特征
- 特征工程完成后，立即运行 05-02 的 quick validation (2 configs × 5 seeds) 验证特征集有效性
