---
phase: 04-stronger-anchor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/exp30_reproduce_anchor.py
  - scripts/exp30_compare_anchors.py
autonomous: false
requirements:
  - ANCHOR-01
must_haves:
  truths:
    - "User has identified 0.966+ public notebooks from Kaggle"
    - "Reproduction script can ingest config and produce a submission"
    - "Reproduced submission is compared against ref 0.96624 with Spearman per horizon"
    - "p48 Spearman between new anchor and 0.96624 is measured for Track 2 eligibility"
  artifacts:
    - path: "scripts/exp30_reproduce_anchor.py"
      provides: "Configurable reproduction script for any RSF/GBSA pipeline"
      min_lines: 80
    - path: "scripts/exp30_compare_anchors.py"
      provides: "Anchor comparison with Spearman per horizon and blend eligibility"
      min_lines: 40
  key_links:
    - from: "scripts/exp30_reproduce_anchor.py"
      to: "scripts/exp17_reproduce_ref.py"
      via: "Reuses feature engineering, postprocessing, SF evaluation patterns"
      pattern: "engineer_features|submission_postprocess"
    - from: "scripts/exp30_compare_anchors.py"
      to: "submissions/"
      via: "Reads CSV submissions for comparison"
      pattern: "pd\.read_csv.*submissions"
---

<objective>
Track 1: Build reproduction infrastructure and get user notebook selections.

Purpose: The single biggest blocker is obtaining a new anchor stronger than 0.96624. Research confirmed Kaggle notebooks are inaccessible via API -- user must manually identify candidates. This plan creates the reproduction tooling and gates on user input.

Output: exp30_reproduce_anchor.py (configurable reproduction), exp30_compare_anchors.py (anchor comparison), user-provided notebook list.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stronger-anchor/04-CONTEXT.md
@.planning/phases/04-stronger-anchor/04-RESEARCH.md
@scripts/exp17_reproduce_ref.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Fork + run target notebooks on Kaggle (OOF export only)</name>
  <action>
    For each public notebook with LB >= 0.966 (besides the known 0.96624 source):
    1. Fork the notebook on Kaggle
    2. Add a single OOF export cell at the end (save OOF predictions to /kaggle/working/oof_preds.csv)
    3. Run the forked notebook — do NOT change any model code
    4. Download 3 artifacts per notebook:
       a. submission.csv (test predictions)
       b. oof_preds.csv (OOF/test predictions)
       c. run_metadata.txt (seed, library versions, data version, LB score)
    5. Save artifacts to submissions/anchor_{notebook_id}/

    If NO notebooks with LB > 0.966 exist, report that — skip to Track 3 (hyperparam grid).

    **Stop-loss rule**: Max 5 Kaggle submissions for Track 1.
    If first 3 submissions all score < 0.965, stop Track 1 and switch to Track 3 immediately.
  </action>
  <how-to-verify>
    1. Go to Kaggle WiDS 2026 competition page -> Code tab
    2. Sort by "Best Score" descending
    3. For each notebook with score >= 0.966: fork, add OOF cell, run, download 3 artifacts
    4. Report back: notebook IDs + LB scores + artifact paths, OR "none found above 0.966"
  </how-to-verify>
  <resume-signal>Provide notebook IDs, LB scores, and artifact paths; or say "none found" / "stop-loss triggered"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build configurable reproduction + comparison scripts</name>
  <files>scripts/exp30_reproduce_anchor.py, scripts/exp30_compare_anchors.py</files>
  <action>
Create two scripts building on exp17_reproduce_ref.py patterns:

**exp30_reproduce_anchor.py** -- Configurable reproduction script:
- Accept CLI args: --config (JSON with feature list, model params, weights, seed, postprocess flags)
- Default config = exact 0.96624 settings (from exp17)
- Pipeline: load data -> feature engineering -> scaler -> train RSF+GBSA -> predict -> postprocess -> save
- Support different RSF/GBSA hyperparams via config
- Output: submission CSV + distribution stats (min/med/max per horizon)
- Reuse engineer_features() and submission_postprocess() from exp17 pattern (copy, don't import)

**exp30_compare_anchors.py** -- Anchor comparison tool:
- Accept CLI args: --sub_a (path) --sub_b (path)
- Compute per-horizon: Spearman rho, max_abs_diff, distribution stats
- Print blend eligibility verdict: "ELIGIBLE" if any horizon has rho < 0.99, "SKIP" otherwise
- Highlight p48 specifically (the horizon that matters most for LB)

Key constraints per CONTEXT.md locked decisions:
- No new features, no model structure changes
- Strict reproduction: same features/CV/seed/postprocess as source
- Use .venv_sksurv22 environment (sksurv 0.22.2)
  </action>
  <verify>
Run: `cd D:/wids && .venv_sksurv22/Scripts/python scripts/exp30_reproduce_anchor.py --help` prints usage.
Run: `cd D:/wids && .venv_sksurv22/Scripts/python scripts/exp30_compare_anchors.py --sub_a submissions/submission_0.96624.csv --sub_b scripts/exp17_reproduced.csv` prints Spearman per horizon and blend eligibility.
  </verify>
  <done>
Both scripts run without error. exp30_reproduce_anchor.py accepts config and produces a submission CSV. exp30_compare_anchors.py compares two submissions and prints Spearman + eligibility verdict.
  </done>
</task>

</tasks>

<verification>
1. exp30_reproduce_anchor.py --help shows configurable parameters
2. exp30_compare_anchors.py correctly computes Spearman between known submissions
3. User has provided notebook list OR confirmed "none found"
</verification>

<success_criteria>
- Reproduction tooling is ready to ingest any notebook configuration
- Anchor comparison tool correctly identifies blend eligibility
- User has provided actionable notebook information for Track 1 execution
</success_criteria>

<output>
After completion, create `.planning/phases/04-stronger-anchor/04-01-SUMMARY.md`
</output>
