---
phase: 04-stronger-anchor
plan: 03
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - scripts/exp30_hyperparam_grid.py
autonomous: false
requirements:
  - ANCHOR-03
gap_closure: true
must_haves:
  truths:
    - "exp30_hyperparam_grid.py postprocess matches exp17_reproduce_ref.py exactly"
    - "Grid produces prob_48h std > 0.30 for all configs"
    - "Top-2 grid configs saved as submission CSVs with valid distributions"
  artifacts:
    - path: "scripts/exp30_hyperparam_grid.py"
      provides: "Fixed RSF hyperparam grid with parity-verified pipeline"
      contains: "submission_postprocess"
    - path: "submissions/submission_exp30_grid_r1.csv"
      provides: "Top-1 grid config submission"
    - path: "submissions/submission_exp30_grid_r2.csv"
      provides: "Top-2 grid config submission"
  key_links:
    - from: "scripts/exp30_hyperparam_grid.py"
      to: "scripts/exp17_reproduce_ref.py"
      via: "postprocess function must be identical (two-round logic)"
      pattern: "submission_postprocess|Round 2"
---

<objective>
Gap closure: fix pipeline parity in exp30_hyperparam_grid.py and re-run grid.

Purpose: The catastrophic LB=0.91089/0.90860 was caused by a postprocess divergence
from the reference pipeline (prob_48h std=0.102 vs ref 0.364). Fix the postprocess
function to exactly match exp17_reproduce_ref.py, then re-run the 4-config grid.

Output: Fixed exp30_hyperparam_grid.py + valid submission CSVs with std > 0.30.

Note: Gaps 1+2 (no accessible anchor above 0.96624, blend never triggered) cannot
be closed by code — Track 1 is exhausted. If the fixed grid produces LB > 0.96624,
Gap 4 is resolved. Otherwise phase 4 remains closed at 0.96624.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-stronger-anchor/04-02-SUMMARY.md
@scripts/exp17_reproduce_ref.py
@scripts/exp30_hyperparam_grid.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix postprocess parity in exp30_hyperparam_grid.py</name>
  <files>scripts/exp30_hyperparam_grid.py</files>
  <action>
Replace the postprocess() function in exp30_hyperparam_grid.py with an exact copy
of submission_postprocess() from exp17_reproduce_ref.py (lines 78-112).

Reference two-round logic:
  Round 1: r[12]=clip(p[12],0.01,0.99); r[24]=p[24]; r[48]=max(p[48],r[24]);
           r[72]=max(p[72],r[48]); r[72]=ones; clip r[24],r[48] to [0.01,0.99]
  Round 2: row-level enforce 12<=24<=48<=72 (prev_val=0.0, iterate EVAL_TIMES);
           clip: non-72 to [0.01,0.99], 72 to [0.01,1.0]

Current exp30_grid postprocess() is missing r[72]=max(p[72],r[48]) before setting
r[72]=ones, and the Round 2 row-level loop order differs from reference.

Changes:
1. Rename postprocess() to submission_postprocess() for clarity
2. Replace body with exact two-round logic from exp17
3. Update call site in run_config() from postprocess(blended) to submission_postprocess(blended)
4. Add distribution check after each config: print prob_48h std; if std < 0.20 print WARNING

Do NOT change GRID configs, GBSA_CFG, FEATURES list, or any other logic.
  </action>
  <verify>
Run: `.venv_sksurv22/Scripts/python scripts/exp30_hyperparam_grid.py --version_check`
Check: REF config (n=200, mf=0.5, msl=5) prints prob_48h std > 0.30.
No "WARNING: distribution compressed" lines in output.
  </verify>
  <done>
exp30_hyperparam_grid.py postprocess matches exp17 exactly. REF config produces
prob_48h std > 0.30. All 4 grid configs produce valid distributions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run grid and save top-2 submissions</name>
  <files>submissions/submission_exp30_grid_r1.csv, submissions/submission_exp30_grid_r2.csv</files>
  <action>
Run the fixed grid:
  .venv_sksurv22/Scripts/python scripts/exp30_hyperparam_grid.py

Script runs 4 configs, sorts by rho deviation from 1.0, saves top-2 as:
  submissions/submission_exp30_grid_r1.csv
  submissions/submission_exp30_grid_r2.csv

After run, verify distributions:
  .venv_sksurv22/Scripts/python -c "import pandas as pd; s=pd.read_csv('submissions/submission_exp30_grid_r1.csv'); print('r1 p48 std:', round(s['prob_48h'].std(),4))"
  .venv_sksurv22/Scripts/python -c "import pandas as pd; s=pd.read_csv('submissions/submission_exp30_grid_r2.csv'); print('r2 p48 std:', round(s['prob_48h'].std(),4))"

Gate: if either CSV shows std < 0.20, do NOT proceed to Task 3 — Task 1 fix is incomplete.
  </action>
  <verify>
Both CSVs exist. Both show prob_48h std > 0.30 in distribution check commands above.
Grid results table printed with rho_p48 for all 4 configs.
  </verify>
  <done>
submission_exp30_grid_r1.csv and submission_exp30_grid_r2.csv contain valid predictions
(std > 0.30). Grid results table shows rho_p48 for each config.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Submit top-2 to Kaggle and record LB scores</name>
  <action>Submit the two grid CSVs to Kaggle and record LB scores. Stop-loss: if LB still < 0.91, pipeline parity fix is incomplete.</action>
  <verify>LB scores reported for both r1 and r2 submissions.</verify>
  <done>LB scores recorded. If any LB > 0.96624: Gap 4 resolved. Otherwise phase 4 remains closed at 0.96624.</done>
  <what-built>Fixed grid pipeline producing valid prediction distributions (std > 0.30).
Two submission CSVs ready for Kaggle.</what-built>
  <how-to-verify>
1. Confirm both CSVs have prob_48h std > 0.30 (from Task 2 output)
2. Submit submissions/submission_exp30_grid_r1.csv to Kaggle — record LB
3. Submit submissions/submission_exp30_grid_r2.csv to Kaggle — record LB
4. If any LB > 0.96624: Gap 4 resolved, phase goal met
5. If both LB near 0.966 range: grid valid but configs don't improve on anchor
6. If LB still < 0.91: pipeline parity fix incomplete — report back
  </how-to-verify>
  <resume-signal>Report LB scores for r1 and r2, or "distributions still invalid" if std check failed</resume-signal>
</task>

</tasks>

<verification>
1. exp30_hyperparam_grid.py submission_postprocess() matches exp17_reproduce_ref.py exactly
2. REF config (n=200, mf=0.5, msl=5) produces prob_48h std > 0.30
3. Both submission CSVs have valid distributions (std > 0.30)
4. Kaggle LB scores reported for both submissions
</verification>

<success_criteria>
- Pipeline parity confirmed: prob_48h std > 0.30 for all grid configs
- LB scores in 0.960-0.970 range (confirming valid pipeline, not catastrophic failure)
- Ideally: at least one submission with LB > 0.96624 (Gap 4 resolved)
</success_criteria>

<output>
After completion, create `.planning/phases/04-stronger-anchor/04-03-SUMMARY.md`
</output>
