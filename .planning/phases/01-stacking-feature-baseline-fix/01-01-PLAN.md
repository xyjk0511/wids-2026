---
phase: 01-stacking-feature-baseline-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [R5]

must_haves:
  truths:
    - "LR head produces valid probabilities for all 3 horizons (no NaN, range 0-1)"
    - "Calibration layer is bypassed -- head output goes directly to postprocessing"
    - "Head uses only 9 base-model prediction columns, not original features"
    - "CV Hybrid Score is recorded as clean baseline for comparison"
  artifacts: []
  key_links:
    - from: "CLI flags"
      to: "train_horizon_heads() parameters"
      via: "argparse wiring at train.py:783-796"
      pattern: "head_model.*logit.*head_base_only.*calibration_mode.*none"
---

<objective>
Establish clean LR-head baseline by running existing pipeline with config flags only.

Purpose: Replace XGB stacking heads with LogisticRegression, remove calibration layer,
restrict head input to base-model predictions only. All changes are CLI flags -- zero
code modifications. This isolates the overfitting reduction from head simplification
before touching features.

Output: CV Hybrid Score baseline number, submission CSV for LB validation.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stacking-feature-baseline-fix/01-CONTEXT.md
@.planning/phases/01-stacking-feature-baseline-fix/01-RESEARCH.md
@src/train.py
@src/stacking.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run LR-head baseline and record CV</name>
  <files></files>
  <action>
Run the pipeline with all four config flags changed from defaults:

```bash
python -m src.train --feature-level v96624_plus --head-model logit --head-base-only --calibration-mode none
```

This exercises existing code paths:
- `--head-model logit` -> stacking.py:274-281 (LogisticRegression C=1.0, balanced, lbfgs)
- `--head-base-only` -> train.py:794 sets use_orig_features=False -> head gets 9 cols (3 models x 3 horizons)
- `--calibration-mode none` -> stacking.py:138-139 returns (None, "none") immediately
- `--feature-level v96624_plus` -> train.py:692 selects 21-feature set from config.py:138-144

Record from output:
1. Head OOF Hybrid Score, CI, WBrier (printed by _print_score at train.py:801)
2. Final submission Hybrid Score after postprocessing
3. Per-horizon Brier scores (B24h, B48h, B72h)
4. 12h probability distribution stats (min, median, max) -- watch for compression

Compare against current default config baseline (XGB head). If CV drops > 0.002, note as risk but proceed -- LB is the true test.

No files are modified. This is a run-and-record task.
  </action>
  <verify>
Command completes without error. Output contains "Head OOF" and "Hybrid=" lines.
Submission CSV is generated at the expected path.
  </verify>
  <done>
CV Hybrid Score recorded for LR-head config. Submission CSV exists for LB upload.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: LB validation of LR-head baseline</name>
  <files></files>
  <action>
User uploads submission CSV to Kaggle and reports LB score.

Decision criteria:
- LB >= 0.96783: clean baseline confirmed, proceed to Plan 02 (feature selection)
- LB >= 0.9670: acceptable, proceed with caution
- LB < 0.9670: investigate probability distribution, may need to adjust C or class_weight
  </action>
  <verify>LB score reported by user.</verify>
  <done>LB score recorded. Go/no-go decision made for Plan 02.</done>
</task>

</tasks>

<verification>
- Pipeline runs to completion with --head-model logit --head-base-only --calibration-mode none
- No errors or NaN in head OOF predictions
- Submission CSV has valid probabilities in [0, 1] for all horizons
- CV metrics recorded for comparison with Plan 02 results
</verification>

<success_criteria>
LR-head CV Hybrid Score recorded. Submission uploaded to Kaggle. LB score >= 0.9670.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stacking-feature-baseline-fix/01-01-SUMMARY.md`
</output>
