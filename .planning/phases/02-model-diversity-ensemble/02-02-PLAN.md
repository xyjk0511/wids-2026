---
phase: 02-model-diversity-ensemble
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/exp32_calibration_methods.py
autonomous: true
requirements: [R1]

must_haves:
  truths:
    - "Isotonic, Platt, and piecewise-linear calibration compared on OOF for 24h and 48h"
    - "Two tracks evaluated: anchor-incremental and independent calibration"
    - "Best calibration method selected by OOF WBrier improvement without CI degradation"
  artifacts:
    - path: "scripts/exp32_calibration_methods.py"
      provides: "Calibration method comparison experiment"
  key_links:
    - from: "scripts/exp32_calibration_methods.py"
      to: "src/evaluation.py"
      via: "hybrid_score and horizon_brier_score for evaluation"
      pattern: "from src\.evaluation import"
    - from: "scripts/exp32_calibration_methods.py"
      to: "submissions/submission_0.96624.csv"
      via: "anchor predictions for incremental track"
      pattern: "submission_0\.96624"
---

<objective>
Compare isotonic, Platt, and piecewise-linear calibration methods on 24h/48h horizons.

Purpose: Improve WBrier via new calibration beyond plateaued logit (A,B). Two tracks: anchor-incremental and independent.
Output: scripts/exp32_calibration_methods.py with OOF comparison and best submission.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-diversity-ensemble/02-CONTEXT.md
@.planning/phases/02-model-diversity-ensemble/02-RESEARCH.md
@src/stacking.py
@src/evaluation.py
@src/labels.py
@src/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement calibration comparison experiment</name>
  <files>scripts/exp32_calibration_methods.py</files>
  <action>
Create scripts/exp32_calibration_methods.py as standalone experiment.

OOF source: Run RSF+EST 5-seed pipeline to get base OOF predictions for 24h and 48h. Use 50/50 blend to isolate calibration effect.

Calibration methods (24h and 48h only, per locked decision):
1. Isotonic: IsotonicRegression(out_of_bounds='clip')
2. Platt: LogisticRegression(C=1.0) on logit(p) -> sigmoid
3. Piecewise-linear: 3-knot spline at p25/p50/p75

Two tracks (per locked decision):
- Track A (anchor-incremental): anchor from 0.96624, new_p = anchor_p + alpha*(cal_p - anchor_p), alpha in [0.1, 0.2, 0.3]
- Track B (independent): calibration applied directly

CV: 5-fold. Fit calibrator on 4 folds OOF, apply to held-out.
Evaluate per method x track x horizon: horizon_brier_score, CI, hybrid_score. Print table.
  </action>
  <verify>
Run: .venv_sksurv22/Scripts/python scripts/exp32_calibration_methods.py
Prints comparison table with WBrier/CI/hybrid for all combinations.
  </verify>
  <done>
All calibration methods compared on OOF. Best method identified per horizon.
  </done>
</task>


<task type="auto">
  <name>Task 2: Generate best calibration submission</name>
  <files>scripts/exp32_calibration_methods.py</files>
  <action>
At end of exp32_calibration_methods.py:

- Select best method+track by OOF hybrid (must not degrade CI vs baseline)
- Apply winning calibration to test predictions for 24h and 48h
- Keep 12h and 72h unchanged from anchor (0.96624 submission)
- Save to submissions/submission_exp32_cal.csv
- Print summary: baseline vs calibrated WBrier/CI/hybrid per horizon
  </action>
  <verify>
submissions/submission_exp32_cal.csv exists with correct columns and row count.
  </verify>
  <done>
Best calibration submission generated. Improvement over baseline quantified.
  </done>
</task>

</tasks>

<verification>
1. Script runs: .venv_sksurv22/Scripts/python scripts/exp32_calibration_methods.py
2. Comparison table printed for all method+track+horizon combinations
3. Best method selected without CI degradation
4. Submission CSV generated with correct format
</verification>

<success_criteria>
- Isotonic, Platt, piecewise-linear compared on 24h/48h OOF
- Both anchor-incremental and independent tracks evaluated
- Best calibration method identified by hybrid score
- Submission generated for LB validation
</success_criteria>

<output>
After completion, create .planning/phases/02-model-diversity-ensemble/02-02-SUMMARY.md
</output>
